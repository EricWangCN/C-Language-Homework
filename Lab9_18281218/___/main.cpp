#include <stdio.h>
#include <string.h>

int count(const char str[], char c);

int main() {
    char a[2],flag[1000],b;
    printf("please input a string:");
    gets(flag);
    printf("please input the word you want to count:");
    gets(a);
    strlrw(a);
    return 0;
}

int count(const unsigned char str[], char c){

}